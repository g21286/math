
set.c:
	SET A has 14 integer elements, [1..14].
	SET B is subset of A.
	If any 3 elements belong to B satisfy a+b!=2c,
		then what is the max number of elements in SET B?

sudoku.c
	A simple text base sudoku game.
	No round data yet.


